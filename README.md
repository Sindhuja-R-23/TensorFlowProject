# TensorFlowProject
Built an object detection system in Python using TensorFlow that classifies MNIST digits and predicts their bounding boxes with a dual-output CNN. Trained the model using custom preprocessing, visualized predictions with IoU evaluation, and implemented full bounding-box rendering on images
